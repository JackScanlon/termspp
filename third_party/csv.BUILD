package(default_visibility = ['//visibility:public'])

licenses(['notice'])
exports_files(['LICENSE'])

cc_library(
  name = 'csv_parser',
  hdrs = ['csv.h'],
  include_prefix = 'fastcsv',
  linkopts = ['-lpthread']
)
